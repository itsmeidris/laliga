import 'package:flutter/cupertino.dart';

class ThemePlayerCard extends StatelessWidget {
  const ThemePlayerCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
